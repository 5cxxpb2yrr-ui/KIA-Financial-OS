export function calculateNetWorth(accounts) { const liabilities = accounts.filter((a) => ['Credit Card','Loan','Mortgage'].includes(a.type)).reduce((sum,a) => sum + a.currentBalance, 0); const assets = accounts.filter((a) => !['Credit Card','Loan','Mortgage'].includes(a.type)).reduce((sum,a) => sum + a.currentBalance, 0); return { assets, liabilities, netWorth: assets - liabilities } }
