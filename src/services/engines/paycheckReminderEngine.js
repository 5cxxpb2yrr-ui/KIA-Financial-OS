export function generatePaycheckReminders({ date, frequency, amount, reminderDays = [2, 1, 0] }) { return reminderDays.map((daysBefore) => ({ id: `${date}-${daysBefore}`, type:'paycheck', date: new Date(new Date(date).getTime() - daysBefore * 86400000).toISOString().slice(0,10), message: daysBefore ? `Paycheck of $${amount} arrives in ${daysBefore} day${daysBefore === 1 ? '' : 's'}.` : `Paycheck of $${amount} arrives today.` })) }
